# Supabase project URL, e.g. https://xxxx.supabase.co
SUPABASE_URL=

# Settings → API Keys → Secret keys → "default" (sb_secret_...)
# SECRET — server-side only, never expose in the browser.
SUPABASE_SECRET_KEY=
